# Challenge 3 — Stegosaurus 2: The Memory Warehouse

> Suggested: finish Challenge 2 first, and keep everything it gave you. This uses
> the exact same photo — but the answer is not in the pixels.

```
TO: MMU

Your TLB is empty. Perform a page-table walk to resolve a virtual address
into a physical location in our memory warehouse.

VA = 0x0000_0100_4040_1005
```

Resolve the address to one box in the warehouse; the note inside tells you the rest.

Flag format: `Flag{...}`
